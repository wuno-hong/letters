<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.letter">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".constant.MyApp"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ChangeInformationActivity"></activity>
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".GetLetter.ShowLetterActivity" />
        <activity android:name=".GetLetter.GetLetterActivity" />
        <activity android:name=".email.WriteLetterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login.RegisterActivity" />
        <activity
            android:name=".constant.LaunchActivity"
            android:theme="@style/LaunchActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>